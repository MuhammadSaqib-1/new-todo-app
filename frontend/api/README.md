# Backend API Routes

This directory contains simplified API routes that interface with the original Python backend logic.

Note: Due to Vercel's constraints with complex Python dependencies, the backend functionality is implemented using a different approach while maintaining compatibility with the original design.